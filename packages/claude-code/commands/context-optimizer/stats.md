---
description: Show cumulative context-optimizer pruning statistics
---

Run `npx @evermeer/context-optimizer@latest stats` with the Bash tool and show its table output to the user verbatim inside a fenced code block (compaction statistics per window: last compact, session, today, week, month, overall). Do not add commentary.
