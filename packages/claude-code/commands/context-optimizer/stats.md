---
description: Show cumulative context-optimizer pruning statistics
---

Run `npx @evermeer/context-optimizer stats` with the Bash tool and show its JSON output to the user as-is (cumulative pruning/compaction statistics across sessions). Do not add commentary.
