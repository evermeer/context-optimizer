---
description: Show cumulative context-optimizer pruning statistics
---

Run `npx @evermeer/context-optimizer@latest stats` with the Bash tool and show its markdown table output to the user verbatim as a rendered markdown table, not inside a code block (compaction statistics per window: last compact, session, today, week, month, overall). Do not add commentary.
